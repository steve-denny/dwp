# Disable spring cloud config client
spring.cloud.config.enabled=false
# Prevent No spring.config.import set error (completely silence config server check)
spring.config.import=optional:configserver:
